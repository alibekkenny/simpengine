basePath: /
definitions:
  auth.LoginRequestDTO:
    properties:
      login:
        minLength: 3
        type: string
      password:
        minLength: 3
        type: string
    required:
    - login
    - password
    type: object
  auth.LoginResponseDTO:
    properties:
      token:
        type: string
    type: object
  media.UploadMediaResponseDTO:
    properties:
      id:
        type: integer
    type: object
  model.ErrorResponse:
    properties:
      code:
        example: INVALID_BODY
        type: string
      message:
        example: Invalid request
        type: string
    type: object
  model.EventStep:
    properties:
      description:
        type: string
      id:
        type: integer
      options:
        items:
          $ref: '#/definitions/model.EventStepOption'
        type: array
      step_order:
        type: integer
      title:
        type: string
    type: object
  model.EventStepOption:
    properties:
      id:
        type: integer
      img_id:
        type: integer
      label:
        type: string
    type: object
  model.RomanticEvent:
    properties:
      description:
        type: string
      event_date:
        type: string
      id:
        type: integer
      public_token:
        type: string
      published_at:
        type: string
      simp_target_id:
        type: integer
      status:
        $ref: '#/definitions/model.RomanticEventStatus'
      title:
        type: string
    type: object
  model.RomanticEventStatus:
    enum:
    - draft
    - published
    - accepted
    - rejected
    - confirmed
    - archived
    type: string
    x-enum-varnames:
    - StatusDraft
    - StatusPublished
    - StatusAccepted
    - StatusRejected
    - StatusConfirmed
    - StatusArchived
  romanticevent.EventStepRequestDTO:
    properties:
      description:
        type: string
      step_order:
        type: integer
      title:
        minLength: 3
        type: string
    required:
    - description
    - step_order
    - title
    type: object
  romanticevent.EventStepResponseDTO:
    properties:
      description:
        type: string
      event_order:
        type: integer
      id:
        type: integer
      title:
        type: string
    type: object
  romanticevent.EventStepsRequestDTO:
    properties:
      steps:
        items:
          $ref: '#/definitions/romanticevent.EventStepRequestDTO'
        type: array
    required:
    - steps
    type: object
  romanticevent.EventStepsResponseDTO:
    properties:
      steps:
        items:
          $ref: '#/definitions/romanticevent.EventStepResponseDTO'
        type: array
    type: object
  romanticevent.RomanticEventRequestDTO:
    properties:
      description:
        type: string
      event_date:
        type: string
      simp_target_id:
        type: integer
      title:
        minLength: 3
        type: string
    required:
    - description
    - event_date
    - simp_target_id
    - title
    type: object
  romanticevent.RomanticEventResponseDTO:
    properties:
      description:
        type: string
      event_date:
        type: string
      id:
        type: integer
      public_token:
        type: string
      published_at:
        type: string
      simp_target_id:
        type: integer
      steps:
        items:
          $ref: '#/definitions/model.EventStep'
        type: array
      title:
        type: string
    type: object
  romanticevent.StepOptionRequestDTO:
    properties:
      img_id:
        type: integer
      label:
        minLength: 3
        type: string
    required:
    - img_id
    - label
    type: object
  romanticevent.StepOptionResponseDTO:
    properties:
      id:
        type: integer
      img_id:
        type: integer
      label:
        type: string
    type: object
  romanticevent.TemplateEventStepRequestDTO:
    properties:
      description:
        type: string
      title:
        minLength: 3
        type: string
    required:
    - description
    - title
    type: object
  romanticevent.TemplateEventStepResponseDTO:
    properties:
      description:
        type: string
      id:
        type: integer
      title:
        type: string
    type: object
  romanticevent.ViewTemplateEventStepResponseDTO:
    properties:
      description:
        type: string
      id:
        type: integer
      options:
        items:
          $ref: '#/definitions/romanticevent.StepOptionResponseDTO'
        type: array
      title:
        type: string
    type: object
  simptarget.CreateSimpTargetResponseDTO:
    properties:
      description:
        type: string
      id:
        type: integer
      name:
        type: string
    type: object
  simptarget.SimpTarget:
    properties:
      description:
        type: string
      id:
        type: integer
      name:
        type: string
    type: object
  simptarget.SimpTargetRequestDTO:
    properties:
      description:
        type: string
      name:
        minLength: 2
        type: string
    required:
    - description
    - name
    type: object
  user.RegisterRequestDTO:
    properties:
      email:
        type: string
      login:
        minLength: 3
        type: string
      password:
        minLength: 3
        type: string
      token:
        description: optional (required only for admins)
        type: string
    required:
    - email
    - login
    - password
    type: object
  user.RegisterResponseDTO:
    properties:
      email:
        type: string
      id:
        type: integer
      login:
        type: string
    type: object
info:
  contact: {}
  description: API for managing romantic events in SimpEngine project
  title: SimpEngine API
  version: "1.0"
paths:
  /admin/romantic-event/template-steps:
    post:
      consumes:
      - application/json
      description: Creates a new template event step (admin only). Template steps
        serve as base reusable steps for romantic events.
      parameters:
      - description: Template event step data
        in: body
        name: step
        required: true
        schema:
          $ref: '#/definitions/romanticevent.TemplateEventStepRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/romanticevent.TemplateEventStepResponseDTO'
        "400":
          description: Invalid request (invalid body or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "403":
          description: Forbidden (insufficient privileges)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Create template event step
      tags:
      - admin_romantic_event
  /admin/romantic-event/template-steps/{id}:
    put:
      consumes:
      - application/json
      description: Updates an existing template event step by ID (admin only).
      parameters:
      - description: Template event step ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      - description: Updated template event step data
        in: body
        name: step
        required: true
        schema:
          $ref: '#/definitions/romanticevent.TemplateEventStepRequestDTO'
      produces:
      - application/json
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request (invalid ID or body)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "403":
          description: Forbidden (insufficient privileges)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Template event step not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update template event step
      tags:
      - admin_romantic_event
  /admin/romantic-event/template-steps/{id}/options:
    post:
      consumes:
      - application/json
      description: Adds an option to an existing template event step (admin only).
      parameters:
      - description: Template event step ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      - description: Step option data
        in: body
        name: option
        required: true
        schema:
          $ref: '#/definitions/romanticevent.StepOptionRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/romanticevent.StepOptionResponseDTO'
        "400":
          description: Invalid request (invalid ID or body)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "403":
          description: Forbidden (insufficient privileges)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Template event step not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Add option to template event step
      tags:
      - admin_romantic_event
  /admin/romantic-event/template-steps/{step_id}/options/{id}:
    put:
      consumes:
      - application/json
      description: Updates an existing option of a template event step (admin only).
      parameters:
      - description: Template event step ID
        format: int64
        in: path
        name: step_id
        required: true
        type: integer
      - description: Step option ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      - description: Updated option data
        in: body
        name: option
        required: true
        schema:
          $ref: '#/definitions/romanticevent.StepOptionRequestDTO'
      produces:
      - application/json
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request (invalid ID or body)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "403":
          description: Forbidden (insufficient privileges)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Template event step or option not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update template event step option
      tags:
      - admin_romantic_event
  /media:
    post:
      consumes:
      - multipart/form-data
      description: Uploads a file to the media storage and returns its ID.
      parameters:
      - description: File to upload
        in: formData
        name: file
        required: true
        type: file
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/media.UploadMediaResponseDTO'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Upload a media file
      tags:
      - media
  /media/{id}:
    delete:
      description: Deletes the file by its id. Only media owner can delete it.
      parameters:
      - description: Media ID
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No content
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: File not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Deletes media file
      tags:
      - media
    get:
      description: Returns the file as binary stream (with correct Content-Type and
        Content-Disposition headers).
      parameters:
      - description: Media ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/octet-stream
      responses:
        "200":
          description: OK
          schema:
            type: file
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: File not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Download media file
      tags:
      - media
  /romantic-event:
    get:
      description: Views a RomanticEvents of current User.
      produces:
      - application/json
      responses:
        "200":
          description: List of RomanticEvents
          schema:
            items:
              $ref: '#/definitions/model.RomanticEvent'
            type: array
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: View RomanticEvents by User
      tags:
      - romantic_event
    post:
      consumes:
      - application/json
      description: Creates romantic event by user
      parameters:
      - description: Romantic event data
        in: body
        name: romanticEvent
        required: true
        schema:
          $ref: '#/definitions/romanticevent.RomanticEventRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/romanticevent.RomanticEventResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Simp target not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Creates romantic event
      tags:
      - romantic_event
    put:
      consumes:
      - application/json
      description: Updates an existing RomanticEvent by ID.
      parameters:
      - description: Romantic event data
        in: body
        name: romanticEvent
        required: true
        schema:
          $ref: '#/definitions/romanticevent.RomanticEventRequestDTO'
      - description: Romantic event id
        format: int64
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: No Content
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Simp target/Romantic event not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "409":
          description: Cannot edit event with given status
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Updates romantic event
      tags:
      - romantic_event
  /romantic-event/{event_id}/steps:
    post:
      consumes:
      - application/json
      description: Add event step to romantic event. Only Romantic event owner, can
        add steps to it.
      parameters:
      - description: Romantic event id
        in: path
        name: event_id
        required: true
        type: integer
      - description: Event step data
        in: body
        name: eventStep
        required: true
        schema:
          $ref: '#/definitions/romanticevent.EventStepsRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/romanticevent.EventStepsResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Romantic event not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Add event step to romantic event
      tags:
      - romantic_event
  /romantic-event/{event_id}/steps/{id}:
    delete:
      consumes:
      - application/json
      description: Removes event step of romantic event. Only Romantic event owner,
        can update step to it.
      parameters:
      - description: Romantic event id
        in: path
        name: event_id
        required: true
        type: integer
      - description: Event step id
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Romantic event/Event step not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Remove event step
      tags:
      - romantic_event
    put:
      consumes:
      - application/json
      description: Updates event step of romantic event. Only Romantic event owner,
        can update step to it.
      parameters:
      - description: Romantic event id
        in: path
        name: event_id
        required: true
        type: integer
      - description: Event step id
        in: path
        name: id
        required: true
        type: integer
      - description: Event step data
        in: body
        name: eventStep
        required: true
        schema:
          $ref: '#/definitions/romanticevent.EventStepRequestDTO'
      produces:
      - application/json
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Romantic event/Event Step not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update event step
      tags:
      - romantic_event
  /romantic-event/{event_id}/steps/{step_id}/options:
    post:
      consumes:
      - application/json
      description: Add option to romantic event step. Only Romantic event owner, can
        add option to event step.
      parameters:
      - description: Romantic event id
        in: path
        name: event_id
        required: true
        type: integer
      - description: Event step id
        in: path
        name: step_id
        required: true
        type: integer
      - description: Step option data
        in: body
        name: stepOption
        required: true
        schema:
          $ref: '#/definitions/romanticevent.StepOptionRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Step option data
          schema:
            $ref: '#/definitions/romanticevent.StepOptionResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Romantic event/Romantic event step not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Add option to event step
      tags:
      - romantic_event
  /romantic-event/{event_id}/steps/{step_id}/options/{id}:
    delete:
      consumes:
      - application/json
      description: Remove option of romantic event step. Only Romantic event owner,
        can remove event step option.
      parameters:
      - description: Romantic event id
        in: path
        name: event_id
        required: true
        type: integer
      - description: Event step id
        in: path
        name: step_id
        required: true
        type: integer
      - description: Id
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Romantic event/Event step/Step option not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Removes event step option
      tags:
      - romantic_event
    put:
      consumes:
      - application/json
      description: Update option of romantic event step. Only Romantic event owner,
        can update event step option.
      parameters:
      - description: Romantic event id
        in: path
        name: event_id
        required: true
        type: integer
      - description: Event step id
        in: path
        name: step_id
        required: true
        type: integer
      - description: Id
        in: path
        name: id
        required: true
        type: integer
      - description: Step option data
        in: body
        name: stepOption
        required: true
        schema:
          $ref: '#/definitions/romanticevent.StepOptionRequestDTO'
      produces:
      - application/json
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Romantic event/Event step/Step option not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update event step option
      tags:
      - romantic_event
  /romantic-event/{id}:
    delete:
      description: Deletes a RomanticEvent by its ID. Only allowed if the user has
        proper permissions.
      parameters:
      - description: SimpTarget ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No Content
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Delete RomanticEvent
      tags:
      - romantic_event
    get:
      description: Views a RomanticEvent by its ID. Users can only see their romantic
        events.
      parameters:
      - description: RomanticEvent ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: RomanticEvent
          schema:
            $ref: '#/definitions/romanticevent.RomanticEventResponseDTO'
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: View RomanticEvent
      tags:
      - romantic_event
  /romantic-event/{id}/publish:
    post:
      consumes:
      - application/json
      description: Creates public token for romantic event and changes it's status
        to published
      parameters:
      - description: RomanticEvent Id
        format: int64
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/romanticevent.RomanticEventResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: RomanticEvent not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Publishes romantic event
      tags:
      - romantic_event
  /romantic-event/options:
    get:
      description: Views all EventOptions of current User.
      produces:
      - application/json
      responses:
        "200":
          description: List of EventStepOptions
          schema:
            items:
              $ref: '#/definitions/model.RomanticEvent'
            type: array
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: View event options by User
      tags:
      - romantic_event
  /romantic-event/template-steps:
    get:
      consumes:
      - application/json
      description: Returns a list of all template event steps available for creating
        romantic events.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/romanticevent.TemplateEventStepResponseDTO'
            type: array
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Get all template event steps
      tags:
      - romantic_event
  /romantic-event/template-steps/{id}:
    get:
      consumes:
      - application/json
      description: Returns details of a template event step with all its options.
      parameters:
      - description: Template event step ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/romanticevent.ViewTemplateEventStepResponseDTO'
        "400":
          description: Invalid request (invalid ID)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Template event step not found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Get template event step by ID
      tags:
      - romantic_event
  /simp-target:
    get:
      description: Views a SimpTargets of current User.
      produces:
      - application/json
      responses:
        "200":
          description: List of SimpTargets
          schema:
            items:
              $ref: '#/definitions/simptarget.SimpTarget'
            type: array
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: View SimpTargets by User
      tags:
      - simp_target
    post:
      consumes:
      - application/json
      description: Creates a simp target by user
      parameters:
      - description: Simp target data
        in: body
        name: simpTarget
        required: true
        schema:
          $ref: '#/definitions/simptarget.SimpTargetRequestDTO'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/simptarget.CreateSimpTargetResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Creates simp target
      tags:
      - simp_target
  /simp-target/{id}:
    delete:
      description: Deletes a SimpTarget by its ID. Only allowed if the user has proper
        permissions.
      parameters:
      - description: SimpTarget ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No Content
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Delete SimpTarget
      tags:
      - simp_target
    get:
      description: Views a SimpTarget by its ID. Users can only see their simp targets.
      parameters:
      - description: SimpTarget ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: SimpTarget
          schema:
            $ref: '#/definitions/simptarget.SimpTarget'
        "400":
          description: Invalid ID
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: View SimpTarget
      tags:
      - simp_target
    put:
      consumes:
      - application/json
      description: Updates an existing SimpTarget by ID.
      parameters:
      - description: SimpTarget ID
        format: int64
        in: path
        name: id
        required: true
        type: integer
      - description: SimpTarget data
        in: body
        name: simpTarget
        required: true
        schema:
          $ref: '#/definitions/simptarget.SimpTargetRequestDTO'
      produces:
      - application/json
      responses:
        "204":
          description: No Content
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update SimpTarget
      tags:
      - simp_target
  /user/login:
    post:
      consumes:
      - application/json
      description: Authenticates a goddamn user
      parameters:
      - description: User data
        in: body
        name: user
        required: true
        schema:
          $ref: '#/definitions/auth.LoginRequestDTO'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.LoginResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "401":
          description: Unauthorized (invalid credentials)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      summary: User login
      tags:
      - user
  /user/register:
    post:
      consumes:
      - application/json
      description: Registers a goddamn user
      parameters:
      - description: User data
        in: body
        name: user
        required: true
        schema:
          $ref: '#/definitions/user.RegisterRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/user.RegisterResponseDTO'
        "400":
          description: Invalid request (invalid ID, invalid body, or validation error)
          schema:
            $ref: '#/definitions/model.ErrorResponse'
        "409":
          description: User with such email or login already exists
          schema:
            $ref: '#/definitions/model.ErrorResponse'
      summary: Register user
      tags:
      - user
schemes:
- https
- http
securityDefinitions:
  BearerAuth:
    in: cookie
    name: jwt
    type: apiKey
swagger: "2.0"
